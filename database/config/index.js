/**
 * Database Configuration Index
 * Entry point for all database-related configurations and utilities
 */

module.exports = {
    // Database connection is configured via env variables
    // See config/db.js for MongoDB connection setup
    
    // Database utilities exported here
    seeders: 'See database/seeders/ folder',
    migrations: 'See database/migrations/ folder',
    samples: 'See database/samples/ folder'
};
